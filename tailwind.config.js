/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      // Using Tailwind defaults - no custom extensions needed
      // This project intentionally uses the default theme
    },
  },
  plugins: [],
}
